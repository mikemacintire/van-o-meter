# Secrets — never commit
.env

# Runtime state and collected data (poller uptime IS the dataset; it lives locally)
data/
logs/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Local tooling
.claude/settings.local.json
.claude/skills/
.impeccable/hook.cache.json
